# go-server

An HTTP server in Go that serves static files.

## Getting Started

```bash
go run .
```

## Usage

Build:
```bash
go build -o server.exe .
```

Run tests:
```bash
go test ./...
```

## Project Structure

- `main.go` — entry point
- `static/` — static assets (HTML, CSS, JS)

## License

MIT
